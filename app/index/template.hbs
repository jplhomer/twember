<ul class="tweets">
  {{#each model as |tweet|}}
    <li>
        {{#tweet-post created_at=tweet.created_at media=tweet.entities.media}}
          {{{tweet.formattedText}}}
        {{/tweet-post}}
    </li>
  {{/each}}
</ul>
